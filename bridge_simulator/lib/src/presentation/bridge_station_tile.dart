import 'package:flutter/material.dart';

class BridgeStationTile extends StatelessWidget {
  const BridgeStationTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
