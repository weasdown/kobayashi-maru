import 'package:flutter/material.dart';

class SimStateView extends StatelessWidget {
  const SimStateView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Simulation State')),
      body: Center(
        child: Text('Control the simulation state on this page - coming soon'),
      ),
    );
  }
}
